//Calculate Area of Given Radius.

#include <stdio.h>
int main()
{ 

    int radius;
    float pi = 3.14;
    printf("enter the radius\n");
    scanf("%d", &radius);
    printf("the area of circle is %f\n", pi * radius * radius);

    return 0;
}
